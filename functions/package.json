{
  "name": "amica-cloud-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test lib/tests/vehicleRatingService.test.js",
    "test:rules:unit": "node --test scripts/vehicle-rules.test.cjs",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/node": "^22.7.5",
    "firebase": "^12.19.0",
    "typescript": "^5.6.3"
  }
}
